# スプレッドシート連携の設定

1. Googleスプレッドシートを新しく作ります。
2. スプレッドシートのメニューから Apps Script を開きます。
3. `google-apps-script.gs` の内容をApps Scriptへ入れます。
   `@OnlyCurrentDoc` が入っているので、このスプレッドシートだけを使う権限になります。
4. Apps Scriptでデプロイを作成し、種類は「ウェブアプリ」にします。
5. 実行ユーザーは「自分」、アクセスできるユーザーは「全員」にします。
6. 発行されたWebアプリURLを、ありがとう日記の「シート連携」に貼って「設定保存」を押します。
   URLは `/exec` で終わる本番URLを使います。`/dev` で終わるテストURLは使いません。

接続テストで「接続OK: ありがとう日記の受信用Webアプリです。version=2026-05-08-sync-load-compat」と表示されれば、最新コードが反映されています。

送信される項目は、受信日時、保存日時、日付、お名前、ありがとう1から3、追加分、全文、送信元です。

同じ日を何度も保存した場合は、保存履歴としてスプレッドシートに行が追加されます。

送信先はスプレッドシート内の `ありがとう日記ログ` タブです。`シート1` ではなく、画面下のタブから `ありがとう日記ログ` を開いて確認してください。

PCや別スマホで送信した記録をアプリ内へ戻したい場合は、同じWebアプリURLと同じお名前を設定してから、日記ツールの `シートから復元` を押してください。履歴、連続日数、今月の達成数がその端末にも反映されます。

Apps Scriptのコードを変更した場合は、デプロイ管理から既存デプロイを編集し、「バージョン」を新しいものに更新してください。コードを保存しただけでは、公開中の `/exec` URLに反映されないことがあります。

接続エラーで `ReferenceError: createResponse_ is not defined` が出る場合は、Apps Scriptに貼ったコードの末尾が欠けています。エディタ内のコードをすべて削除し、`google-apps-script.gs` を先頭から末尾まで丸ごと貼り直してから、新しいバージョンで再デプロイしてください。
